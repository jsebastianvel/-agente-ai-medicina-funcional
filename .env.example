GEMINI_API_KEY=

# Optional overrides. CHROMA_DATA_DIR defaults to C:\venvs\agente_ai_data\chroma_data
# on this machine (outside OneDrive-synced Documents, where Python's own file APIs
# fail to create new directories). Override for Docker/deploy, e.g. ./data/chroma.
CHROMA_DATA_DIR=
